# Video Compressor (Tkinter + FFmpeg)

## Requirements
- Python 3.x
- **FFmpeg + FFprobe** binaries must be placed here:
  - `./ffmpeg/ffmpeg.exe`
  - `./ffmpeg/ffprobe.exe`

## Install (Python)
```bash
pip install -r requirements.txt
```
## Build
``` bash
python -m PyInstaller app.py
```